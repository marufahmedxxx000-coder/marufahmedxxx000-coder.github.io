/ (root)
├─ index.html        ← Customer-facing shop (প্রোডাক্ট লিস্ট + order form)
├─ admin.html        ← Admin panel (প্রোডাক্ট ম্যানেজ + orders)
├─ css/
│   └─ style.css
├─ js/
│   └─ app.js
└─ assets/
    └─ (বা এখানে product images রাখতে পারো; কিন্তু এখানে আমরা আপলোড করলে base64-এ localStorage এ রাখবো)
